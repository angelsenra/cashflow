name: CI
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.10.3
      - name: Cache Python dependencies
        uses: actions/cache@v2
        env:
          cache-name: cashflow-cache-pip
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-${{ github.job }}-${{ env.cache-name }}-${{ hashFiles('Pipfile', 'Pipfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ github.job }}-${{ env.cache-name }}-
            ${{ runner.os }}-${{ github.job }}-
            ${{ runner.os }}-
      - name: Install Python dependencies
        run: |
          cd backend
          pip install --user pipenv
          echo "Installed pipenv."
          pipenv install
          pwd
          ls -la
          pipenv sync
          echo "Installed pipenv requirements."
          pipenv run pip freeze
      - name: Run Linting
        run: |
          cd backend
          pipenv run pip freeze
          pipenv install
          pipenv run pip freeze
          pipenv run black . --check --diff
          echo "Ran black."
          pipenv run isort --diff -rc .
          echo "Ran isort."
          pipenv run pylint .
          echo "Ran pylint."
          pipenv run mypy .
          echo "Ran mypy."
